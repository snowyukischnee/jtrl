package main

func main() {
	//width := 200
	//height := 100
	//img := utils.NewImage(width, height)
	//for x := 0; x < width; x++ {
	//	for y := 0; y < height; y++ {
	//		switch {
	//		case x < width/2 && y < height/2:
	//			img.Paint(x, y, core.NewVec3(100,200,0))
	//		case x >= width/2 && y >= height/2:
	//			img.PaintC(x, y, color.White)
	//		default:
	//		}
	//	}
	//}
	//img.WriteImage("image.png")
}
